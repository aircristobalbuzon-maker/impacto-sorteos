import {requireAdmin} from '@/lib/supabase/server'
export default async function Page(){const{ supabase:db}=await requireAdmin();const{data:rows}=await db.from('participants').select('*,tickets(count),purchases(count)').order('created_at',{ascending:false});return <><span className="eyebrow">BASE DE DATOS</span><h1>Participantes</h1><div className="card"><table><thead><tr><th>Nombre</th><th>Documento</th><th>WhatsApp</th><th>Correo</th></tr></thead><tbody>{rows?.map((x:any)=><tr key={x.id}><td>{x.full_name}</td><td>{x.document}</td><td>{x.whatsapp}</td><td>{x.email||'—'}</td></tr>)}</tbody></table></div></>}
