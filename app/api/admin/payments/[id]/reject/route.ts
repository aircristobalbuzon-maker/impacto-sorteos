import {NextResponse} from 'next/server';import {requireAdmin} from '@/lib/supabase/server'
export async function POST(req:Request,{params}:{params:Promise<{id:string}>}){try{const{id}=await params;const{ supabase,user}=await requireAdmin();const{error}=await supabase.from('payments').update({status:'REJECTED',reviewed_by:user.id,reviewed_at:new Date().toISOString()}).eq('id',id).eq('status','PENDING');if(error)throw error;await supabase.from('audit_logs').insert({admin_id:user.id,action:'PAYMENT_REJECTED',entity_type:'payment',entity_id:id});return NextResponse.redirect(new URL('/admin/pagos',req.url),303)}catch{return NextResponse.json({error:'No se pudo rechazar.'},{status:400})}}
